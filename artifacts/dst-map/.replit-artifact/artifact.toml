kind = "web"
previewPath = "/"
title = "DST World Map"
version = "1.0.0"
id = "artifacts/dst-map"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25661

[services.development]
run = "pnpm --filter @workspace/dst-map run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/dst-map", "run", "build" ]
publicDir = "artifacts/dst-map/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25661"
BASE_PATH = "/"
